{
  "name": "ovid-notes-example",
  "version": "0.0.0",
  "private": true,
  "description": "Sample notes app used to demonstrate ovid (Flask API + SQLite + vanilla web UI + CLI)",
  "devDependencies": {
    "@srinivasa314/ovid": "^0.0.3"
  }
}
